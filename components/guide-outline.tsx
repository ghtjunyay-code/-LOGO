import {guideAngle,type EmbroideryGuide} from '@/lib/embroidery-guide';
export default function GuideOutline({guide,background,x,y,width}:{guide:EmbroideryGuide;background:string;x:number;y:number;width:number}){if(guide.kind==='基準なし')return null;const rgb=[1,3,5].map(i=>parseInt(background.slice(i,i+2),16));const stroke=rgb[0]*.299+rgb[1]*.587+rgb[2]*.114>145?'#344454':'#e1e8ef';return <g transform={`translate(${x} ${y}) rotate(${guideAngle(guide)})`} fill="none" stroke={stroke} strokeWidth={width/130}><line x1={-width/2} x2={width/2} y1="0" y2="0" strokeDasharray={guide.kind==='縫い目'?'2 2':undefined}/>{guide.kind==='ポケット'&&<path d={`M ${-width/2} 0 V ${width*.28} Q ${-width/2} ${width*.4} ${-width*.35} ${width*.4} H ${width*.35} Q ${width/2} ${width*.4} ${width/2} ${width*.28} V 0`}/>}</g>}
